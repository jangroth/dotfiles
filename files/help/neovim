

## Leader & general

Leader key is `Space`.

| Key          | Action                        |
|--------------|-------------------------------|
| `Space+e`    | Toggle file explorer (sidebar)   |
| `Space+E`    | Toggle file explorer (floating)  |
| `Space+h`    | Toggle search highlight       |
| `Space+i`    | Toggle ignore case            |
| `Space+w`    | Toggle word wrap              |

## File finding (Telescope)

| Key          | Action                        |
|--------------|-------------------------------|
| `Space+ff`   | Find files by name            |
| `Space+fF`   | Find files by name, including gitignored |
| `Space+fg`   | Live grep (search file contents) |
| `Space+fG`   | Live grep, including gitignored content |
| `Space+fb`   | Browse open buffers           |

Inside Telescope: type to filter, `↑/↓` or `Ctrl+k/j` to move, `Enter` to open, `Ctrl+v` for vertical split, `Ctrl+x` for horizontal split.

Note: live grep requires `ripgrep` (`brew install ripgrep`).

## File explorer (neo-tree)

Hidden and git-ignored files are shown by default. Use `H` to toggle them.

Sources: filesystem, buffers, git status, document symbols (LSP). Click a tab in the winbar, or cycle with `Tab`/`Shift+Tab` from inside the tree window.

| Key     | Action                        |
|---------|-------------------------------|
| `Tab`   | Next source                   |
| `Shift+Tab` | Previous source            |
| `Enter` | Open file                     |
| `s`     | Open in vertical split        |
| `S`     | Open in horizontal split      |
| `a`     | Create new file or directory  |
| `d`     | Delete file                   |
| `r`     | Rename file                   |
| `P`     | Preview file                  |
| `gy`    | Copy absolute path            |
| `H`     | Toggle hidden/gitignored files |
| `R`     | Refresh                       |
| `?`     | Show all keybindings          |

## Splits & pane navigation

| Key            | Action                                      |
|----------------|---------------------------------------------|
| `:vs`          | Vertical split                              |
| `:sp`          | Horizontal split                            |
| `Ctrl+h/j/k/l` | Move between splits and tmux panes          |
| `Ctrl+w` then `<`/`>` | Shrink / widen the focused split     |

## LSP

| Key    | Action                        |
|--------|-------------------------------|
| `gd`   | Go to definition              |
| `gr`   | Find references               |
| `K`    | Hover documentation           |
| `[d`   | Previous diagnostic           |
| `]d`   | Next diagnostic               |

LSP servers: `pyright`/`ruff` (Python), `yamlls` (YAML), `marksman` (Markdown — go-to-definition/completion for links and references, diagnostics for broken links).

## Markdown (markview.nvim)

Headings, lists, tables, and code blocks render in-buffer automatically when opening a `.md` file — no separate preview window needed.

| Key         | Action                        |
|-------------|--------------------------------|
| `Space+mv`  | Toggle markdown preview rendering |

## Git changes (gitsigns)

Sign column shows `+`/`~`/`_` for added/changed/removed lines. Current line blame is always on.

| Key         | Action                        |
|-------------|--------------------------------|
| `]c`        | Next git hunk                 |
| `[c`        | Previous git hunk              |
| `Space+gp`  | Preview hunk diff              |
| `Space+gs`  | Stage hunk                     |
| `Space+gr`  | Reset hunk                     |
| `Space+gb`  | Toggle current line blame      |

## Commenting (Comment.nvim)

| Key   | Action                        |
|-------|-------------------------------|
| `gcc` | Toggle comment on current line |
| `gc`  | Toggle comment on selection (visual mode) |

## Search

Search results are always centered (`n`, `N`, `*`, `#` all auto-`zz`).

| Key | Action                                  |
|-----|-----------------------------------------|
| `*` | Search for word under cursor (forward)  |
| `#` | Search for word under cursor (backward) |

## Marks and jumps

| Key        | Action                                  |
|------------|-----------------------------------------|
| `ma`       | Set mark `a` at cursor (any letter a–z) |
| `'a`       | Jump to line of mark `a`               |
| `` `a ``   | Jump to exact position of mark `a`     |
| `'.`       | Jump to position of last edit          |
| `Ctrl+O`   | Jump back (older position)             |
| `Ctrl+I`   | Jump forward (newer position)          |

Tip: after searching or jumping to a definition, `Ctrl+O` brings you right back.

## Useful motions

| Key      | Action                                          |
|----------|-------------------------------------------------|
| `ci"`    | Change inside quotes                            |
| `ca(`    | Change around parentheses (includes the parens) |
| `dit`    | Delete inside HTML/XML tag                      |
| `=G`     | Re-indent from cursor to end of file            |
| `Ctrl+A` | Increment number under cursor                   |
| `Ctrl+X` | Decrement number under cursor                   |

## Macros

| Key   | Action                            |
|-------|-----------------------------------|
| `qa`  | Start recording into register `a` |
| `q`   | Stop recording                    |
| `@a`  | Replay macro `a`                  |
| `@@`  | Repeat last replayed macro        |
| `5@a` | Replay macro `a` five times       |
